'Namespace AWB.Plugins.SDKSoftware.Kingbotk.Components
Friend Class PluginSettings
    '* General options/display:
    ' open skipped list (skipped for tagging errors list; build as HTML with edit links)
    ' tag categories option (where a suitable class= exists; maybe should be in template-specific options)
    ' hide AWB tabs (may be a menu item)
    ' do AWB settings (basically turn everything off)
    ' reset #s when get a reset event
    ' duration, edits, rate
    ' load/save settings buttons
    ' stop/start
End Class
'end namespace